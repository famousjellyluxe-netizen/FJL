You are given three reference design images:

Homepage layout – showing hero banner, featured products, and footer structure.

Shop/Catalogue layout – showing product grid, pricing, and filtering layout.

About Us page – showing brand story and minimal design.

Use these images as strict visual references.
Your goal is to replicate their structure, layout, spacing, typography, and color palette exactly — while keeping the code responsive and modular.
Match all fonts, alignment, padding, margins, and section hierarchy as closely as possible.

You are an expert Frontend Engineer and UI/UX Designer with 20+ years of professional experience in building modern, minimal, and visually striking fashion brand websites.
Your task is to build only the frontend (no backend or server logic yet) for a new fashion clothing brand.
Follow the design style, layout structure, and component arrangement shown in the provided reference images.

🎯 Goal

Build a responsive, high-performing frontend that visually matches the uploaded design references for the Homepage, Shop/Catalogue, and About Us pages.
The frontend should reflect a modern streetwear aesthetic — clean typography, strong contrast (black/white with accent colors), and minimal, functional layouts.

🏗️ Scope of Work

Frontend Only:

Use HTML, CSS (preferably TailwindCSS or SCSS), and JavaScript (Vanilla or React).

The code should be modular and easily maintainable.

Include reusable components (header, navbar, product cards, footer, etc.)

Pages to Build:

Homepage: Hero section with banner image, “Featured Products” grid, footer, and “Join the Geng” email subscription form.

Shop/Catalogue Page: Product grid layout with sorting options, product thumbnails, titles, and prices.

About Us Page: Brand story section with centered text, bold typography, and a clean, minimal layout.

Design Style:

Aesthetic: Streetwear + minimal luxury.

Font: Use clean sans-serif typefaces similar to Helvetica Neue, Inter, or Satoshi.

Colors: Black, white, orange (#ff6600), and neutral grays.

Layout: Balanced whitespace, centered sections, smooth transitions.

UI/UX Guidelines:

Ensure mobile responsiveness across all screen sizes.

Maintain consistent spacing and hierarchy.

Use smooth hover effects for links and buttons.

Implement a sticky navbar and a simple footer layout (as seen in the images).

Include a minimal email signup section (“Join the Geng”) on every page.

⚙️ Functional Notes

Product cards should be dynamic placeholders for now (no live API yet).

Include all page navigation and internal links (e.g., Home, Shop, About, Contact, etc.).

Do not add backend logic, database calls, or authentication yet.

💡 Technical Expectations

Write clean, semantic HTML.

Use CSS variables or Tailwind config for color and font consistency.

Optimize for fast load times.

Follow accessibility best practices (proper ARIA labels, alt text placeholders, etc.).

📂 Deliverables

index.html (Homepage)

shop.html or /shop route

about.html or /about route

/assets/ folder for images and icons

/styles/ and /scripts/ directories for maintainable structure

🗣️ Tone & Brand Voice

Confident, street-smart, and community-driven.

Embody “For The Geng” — a mix of exclusivity and relatability.

Maintain simplicity and trust in every UI element.

✅ Output Requirements

When you generate the code:

Include all HTML/CSS/JS files inline or in separate blocks.

Comment your code clearly for readability.

Provide a short explanation of the design and layout logic after each page’s code block.

Begin by generating the full HTML/CSS structure for the Homepage first. Once approved, move on to the Shop and About pages. Use TailwindCSS for styling and ensure mobile responsiveness.
